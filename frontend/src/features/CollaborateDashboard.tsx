import { HStack, VStack } from "@chakra-ui/react";
import { useStore } from "../store";

function CollaborateDashboard() {
    const { } = useStore();

    return (
        <VStack>
            
        </VStack>
    );
}
